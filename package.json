{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "watch": "vite build --watch",
        "start": "start php artisan serve --port=8001 && start npm run watch",
        "kill": "taskkill /IM php.exe /F && taskkill /IM node.exe /F",
        "clear-all": "php artisan config:clear && php artisan route:clear && php artisan cache:clear && npm run start"
    },
    "devDependencies": {
        "@popperjs/core": "^2.11.6",
        "@vitejs/plugin-vue": "^4.5.0",
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "sass": "^1.56.1",
        "vite": "^5.0.0"
    },
    "dependencies": {
        "bootstrap": "^5.3.3"
    }
}
